import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'audiobook_anything',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
